# GhestShop — environment template. Copy to `.env` and fill in real values.

# PostgreSQL connection string consumed by Prisma (schema.prisma `datasource db`).
DATABASE_URL="postgresql://USER:PASSWORD@localhost:5432/ghestshop?schema=public"

# Node environment (development | production)
NODE_ENV="development"
